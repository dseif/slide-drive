# Slide-drive #

Slide-drive is an HTML5 slideshow tool that let's audio/video drive to content of the slideshow. The slideshow author needs nothing more than a basic understanding of HTML/CSS to make a fully featured slideshow.

Some useful features are:

* spacebar/backspace, left/right arrow keys to go forward/backward in the slides
